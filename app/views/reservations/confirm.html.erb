<h1>予約内容の確認</h1>

<%= form_with model:@reservation, url: reservations_path do |f| %>
  <tr>チェックイン</tr>
  <td><%= @reservation.start_date %></td>
  <td><%= f.hidden_field :start_date %></td>
  <br>
  <tr>チェックアウト</tr>
  <td><%= @reservation.end_date %></td>
  <td><%= f.hidden_field :end_date %></td>
  <br>
  <tr>人数</tr>
  <td><%= @reservation.how_many_people %></td>
  <td><%= f.hidden_field :how_many_people %></td>
  <br>
  <tr>宿泊日数</tr>
  <td><%= @reservation.total_days = (@reservation.end_date - @reservation.start_date)/86400 %></td> 
  <td><%= f.hidden_field :total_days %></td>
  <br>
  <tr>合計金額</tr>
  <td><%= @reservation.total_price = ((@reservation.total_days + @reservation.how_many_people ) * @rooms.price) %></td>
  <td><%= f.hidden_field :total_price %></td>
  <br>

  上記内容で正しければ「確定」ボタンを押してください。
  <%= f.submit "確定" %>
<% end %>
